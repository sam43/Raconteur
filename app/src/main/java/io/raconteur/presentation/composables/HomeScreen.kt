package io.raconteur.presentation.composables

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

@Composable
fun HomeNavigation(modifier: Modifier = Modifier) {
    // bottom navigation tabs Home, Chat, Profile and other child composables
    // Media picker,
    // real time Preview (bottom sheet)
    // aspect ratio Presets
    // smart crop assistant
    // one tap export (save to gallery)
    // chat tab, and profile tab
    // internal ML kit for face detection (firebase ML kit)
}